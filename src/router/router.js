import Vue from 'vue'
import Router from 'vue-router'
import routes from './routes'

export default () => {
  return new Router({
    routes
  })
}

// Vue.use(Router)
// export default new Router({
//   routes
// })
